{
  "name": "@igloo-be/amaging-client",
  "version": "0.1.1",
  "main": "lib/client.js",
  "scripts": {
    "prepare-test": "rm -rf .tmp && mkdir -p .tmp/storage && cp -r test/expected/* ./.tmp/storage",
    "test": "npm run prepare-test && nyc mocha",
    "lint": "standard --env mocha"
  },
  "engines": {
    "node": ">=6"
  },
  "devDependencies": {
    "@igloo-be/amaging": "^1.4.0",
    "chai": "^4.1.2",
    "chai-fs": "2.0.0",
    "mocha": "^5.2.0",
    "nyc": "~13.0.1",
    "standard": "~12.0.1"
  },
  "dependencies": {
    "debug": "^4.0.1",
    "@igloo-be/amaging-policy": "^3.0.0",
    "mime": "^2.3.1",
    "request": "^2.88.0",
    "url-join": "4.0.0"
  }
}
