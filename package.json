{
  "name": "@igloo-be/amaging-client",
  "version": "0.1.0",
  "main": "lib/client.js",
  "scripts": {
    "prepare-test": "rm -rf .tmp && mkdir -p .tmp/storage && cp -r test/expected/* ./.tmp/storage",
    "test": "npm run prepare-test && nyc mocha",
    "lint": "standard --env mocha"
  },
  "engines": {
    "node": ">=6"
  },
  "devDependencies": {
    "@igloo-be/amaging": "^1.0.0",
    "chai": "^3.4.1",
    "chai-fs": "0.1.0",
    "mocha": "^3.5.3",
    "nyc": "~11.0.2",
    "standard": "~10.0.2"
  },
  "dependencies": {
    "debug": "^2.2.0",
    "@igloo-be/amaging-policy": "^1.0.1",
    "lodash": "^3.10.1",
    "mime": "^1.3.4",
    "moment": "^2.10.6",
    "request": "^2.67.0",
    "url-join": "0.0.1"
  }
}
